/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "openweathermap.org",
      "wallpapers.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
